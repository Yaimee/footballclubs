namespace app{
    class Test{
        static void Main(){
            /*
            Club myClub = clubs[0];
            System.Console.WriteLine(myClub.name);
            myClub.streakSet("W");
            myClub.streakSet("W");
            myClub.streakSet("W");
            myClub.streakSet("W");
            myClub.streakSet("Ww");
            System.Console.WriteLine(myClub.streak);
            myClub.streakSet("L");
            System.Console.WriteLine(myClub.streak);
            */
        }
    }
}